#include <stdio.h>

/**
 * main - prints its name, followed by a new inr.
 * @argc: number of command ine arguments.
 * @argv: array that contains the program command line arguments.
 * Return: 0 - success.
 */

int main(int argc _attribute_((unused)), char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
